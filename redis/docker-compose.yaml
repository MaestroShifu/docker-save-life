version: '3.7'

services:
  redis:
    container_name: Redis_5 
    image: redis:6-alpine
    volumes:
      - redis_vol:/data
    ports: 
      - 6379:6379
    restart: always
    network_mode: bridge

volumes:
  redis_vol:
